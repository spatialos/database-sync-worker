{
    "workerType": "GameLogic",
    "layer": "gamelogic",
    "entityInterest": {
      "range_entity_interest": {
        "radius": 100
      }
    },
    "permissions": {
        "all": {}
    },
    "componentDelivery": {
      "default": "RELIABLE_ORDERED",
      "checkout_all_initially": true
    },
    "launchConfiguration": {
      "cloudDeployment": {
        "linux": {
          "localBuildDirectory": "../Workers/GameLogic/bin/x64/Release/netcoreapp2.2/linux-x64/publish",
          "command": "GameLogic",
          "arguments": [
            "receptionist",
            "--spatialos-host", "${IMPROBABLE_RECEPTIONIST_HOST}",
            "--spatialos-port", "${IMPROBABLE_RECEPTIONIST_PORT}",
            "--worker-name", "${IMPROBABLE_WORKER_ID}",
            "--logfile", "${IMPROBABLE_LOG_FILE}",
            "--deployment-name", "${IMPROBABLE_DEPLOYMENT_NAME}"
          ]
        }
      },
      "localDeployment": {
        "macos": {
          "localBuildDirectory": "../Workers/GameLogic/bin/x64/Debug/netcoreapp2.2/local",
          "command": "sh",
          "arguments": [
            "LocalRun.sh",
            "receptionist",
            "--spatialos-host", "${IMPROBABLE_RECEPTIONIST_HOST}",
            "--spatialos-port", "${IMPROBABLE_RECEPTIONIST_PORT}",
            "--worker-name", "${IMPROBABLE_WORKER_ID}",
            "--logfile", "${IMPROBABLE_LOG_FILE}",
            "--deployment-name", "${IMPROBABLE_DEPLOYMENT_NAME}"
          ]
        },
        "linux": {
          "localBuildDirectory": "../Workers/GameLogic/bin/x64/Debug/netcoreapp2.2/local",
          "command": "sh",
          "arguments": [
            "LocalRun.sh",
            "receptionist",
            "--spatialos-host", "${IMPROBABLE_RECEPTIONIST_HOST}",
            "--spatialos-port", "${IMPROBABLE_RECEPTIONIST_PORT}",
            "--worker-name", "${IMPROBABLE_WORKER_ID}",
            "--logfile", "${IMPROBABLE_LOG_FILE}",
            "--deployment-name", "${IMPROBABLE_DEPLOYMENT_NAME}"
          ]
        },
        "windows": {
          "localBuildDirectory": "../Workers/GameLogic/bin/x64/Debug/netcoreapp2.2/local",
          "command": "cmd",
          "arguments": [
            "/c",
            "LocalRun.cmd",
            "receptionist",
            "--spatialos-host", "${IMPROBABLE_RECEPTIONIST_HOST}",
            "--spatialos-port", "${IMPROBABLE_RECEPTIONIST_PORT}",
            "--worker-name", "${IMPROBABLE_WORKER_ID}",
            "--logfile", "${IMPROBABLE_LOG_FILE}",
            "--deployment-name", "${IMPROBABLE_DEPLOYMENT_NAME}"
          ]
        }
      }
    }
  }
